version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "first_App-CompleteSetUp"
        LoadBalancerInfo:
          ContainerName: "flask-login-app"
          ContainerPort: 5000
